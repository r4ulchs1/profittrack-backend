spring:
  application:
    name: ProfitTrack

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:5432/postgres}
    username: ${DATABASE_USER:postgres.waxivkbmxwfhkwqdpaag}
    password: ${DATABASE_PASSWORD:Camarones0902200}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.jdbc.lob.non_contextual_creation: true

app:
  jwt:
    secret: ${JWT_SECRET:XJN2vHu7vgOBZ143nqyHf99xhTkYlqNTRVFH5NgENiE=}
    access-expiration-seconds: 900
    refresh-expiration-seconds: 604800
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000, https://core.rrelease.tech, https://profittrack-frontend.vercel.app/}

server:
  forward-headers-strategy: framework
  port: ${PORT:8080}