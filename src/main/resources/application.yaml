spring:
  application:
    name: ProfitTrack

  datasource:
    url: jdbc:postgresql://localhost:5432/profittrack
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.jdbc.lob.non_contextual_creation: true

app:
  jwt:
    secret: "XJN2vHu7vgOBZ143nqyHf99xhTkYlqNTRVFH5NgENiE="
    access-expiration-seconds: 900
    refresh-expiration-seconds: 604800
  cors:
    allowed-origins: "http://localhost:3000"

server:
  port: ${PORT:8080}